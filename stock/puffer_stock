#!/bin/bash
# ./puffer with the stock-NLE backend loaded in place of the fork library. Run from the PufferLib root; needs NLE_STOCK_LIB and NETHACKDIR (stock's).
exec env LD_LIBRARY_PATH="vendor/fast-nle/stock/build${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}" ./puffer "$@"
